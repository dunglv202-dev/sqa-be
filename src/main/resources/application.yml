spring:
  datasource:
    url: jdbc:mysql://0.tcp.ap.ngrok.io:19795/sqa?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true